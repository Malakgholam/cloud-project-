apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticket-service
  namespace: supportdesk-prod
  labels:
    app: ticket-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ticket-service
  template:
    metadata:
      labels:
        app: ticket-service
    spec:
      containers:
        - name: ticket-service
          image: supportdesk/ticket-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "3001"
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: mongo-secret
                  key: tickets-uri
            - name: NOTIFICATION_SERVICE_URL
              value: "http://notification-service:3003"
            - name: LOG_LEVEL
              value: "error"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: ticket-service
  namespace: supportdesk-prod
  labels:
    app: ticket-service
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
  selector:
    app: ticket-service